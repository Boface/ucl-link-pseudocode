import { MatchInterface } from './interfaces';

export class MatchingService implements MatchInterface {


    like() {
        // Like a user
    }

    link() {
        // Link with a user
    }

    match() {
        // Match with a user
    }

    unmatch() {
        // Unmatch with a user
    }

    unlike() {
        // Unlike a user
    }

    unlink() {
        // Unlink with a user
    }
}